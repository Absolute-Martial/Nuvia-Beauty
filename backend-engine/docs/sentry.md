# Sentry

## Rules

- Configure DSN through environment only
- Scrub API keys, tokens, uploaded file paths, and private user data
- Tag events with service name, environment, and release
- Do not attach uploaded photos to error reports

